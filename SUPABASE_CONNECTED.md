# Supabase connection

Project You+ is connected to Supabase project `ehyewrldhttmijizdwhs` in `us-east-1`.

- Project URL: `https://ehyewrldhttmijizdwhs.supabase.co`
- Live account mode: `NEXT_PUBLIC_DEMO_MODE=false`
- Core schema applied successfully
- Row Level Security enabled on all user-data tables
- Security advisor scan: clean after trigger hardening

The publishable client key is configured locally in `.env.local`. Vercel still needs the same environment variables configured in its project settings when deployed.
